# EMC Toolkit AI runtime

This directory is reserved for the offline AI runtime files packaged by the installer.
The application uses the fixed local model `qwen2.5:7b` and does not expose model
selection or runtime setup instructions in the production UI.
